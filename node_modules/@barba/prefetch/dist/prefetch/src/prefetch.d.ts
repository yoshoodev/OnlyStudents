/**
 * @barba/prefetch
 * <br><br>
 * ## Barba prefetch.
 *
 * @module prefetch
 * @preferred
 */
import { IPrefetchOptions } from './defs';
/***/
import { Core } from '@barba/core/src/core';
import { IBarbaPlugin } from '@barba/core/src/defs';
import { Logger } from '@barba/core/src/modules/Logger';
declare class Prefetch implements IBarbaPlugin<IPrefetchOptions> {
    name: string;
    version: string;
    barba: Core;
    logger: Logger;
    observer: IntersectionObserver;
    root: HTMLElement | HTMLDocument;
    timeout: number;
    toPrefetch: Set<string>;
    /**
     * Plugin installation.
     */
    install(barba: Core, { root, timeout }?: IPrefetchOptions): void;
    /**
     * Plugin initialisation.
     */
    init(): void;
    observe(): void;
}
declare const prefetch: Prefetch;
export default prefetch;
